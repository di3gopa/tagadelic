<?php

/**
 * @file
 * Contains basic hooks for tagadelic cloud.
 */

/**
 * Implements hook_theme().
 */
function tagadelic_theme($existing, $type, $theme, $path) {
  return array(
    'tagadelic_taxonomy_cloud' => array(
      'variables' => array(
        'tags' => array(),
        'name' => '',
      ),
    ),
  );
}
